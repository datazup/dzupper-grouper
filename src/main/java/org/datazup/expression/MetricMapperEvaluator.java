/*
package org.datazup.expression;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

*/
/**
 * Created by admin@datazup on 11/26/16.
 *//*

public class MetricMapperEvaluator extends SimpleObjectEvaluator {
    private static final Logger LOG = LoggerFactory.getLogger(MetricMapperEvaluator.class);

    public final static Function SUM = new Function("SUM", 1);


    private static MetricMapperEvaluator INSTANCE;

    public static MetricMapperEvaluator getInstance(){
        synchronized (MetricMapperEvaluator.class){
            if (null==INSTANCE){
                synchronized (MetricMapperEvaluator.class){
                    if (null==INSTANCE)
                        INSTANCE = new MetricMapperEvaluator();
                }
            }
        }
        return INSTANCE;
    }

}
*/
